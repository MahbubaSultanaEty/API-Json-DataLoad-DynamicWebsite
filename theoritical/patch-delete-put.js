/*3. PUT

Purpose: Existing resource completely update korte use hoy.
Body: Required (full new data).
 Idempotent: ✅ (Same request multiple times → same result).
Example: Update user info
*/

/*4. PATCH

Purpose: Existing resource partially update korte use hoy.
Body: Required (only fields to update).
Idempotent:  (Usually, same request same result).
Example: Update only ag
*/

/*5. DELETE

Purpose: Resource delete korte use hoy.
Body: Usually not needed.
Idempotent: ✅ (Deleting again doesn’t change outcome).
Example: Delete user
*/